import React from 'react';
import './{{pascalCase name}}.styles.scss';

/**
 * {{pascalCase name}} component description
 */

type {{pascalCase name}}Props = {};

const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = (/* {} */) => (
  <div className="{{pascalCase name}}">
    <p>
      Edit <code>src/components/{{pascalCase name}}.tsx</code> and save to reload.
    </p>
  </div>
);

export default {{pascalCase name}};
